# User Prompt
suffix timestamp like 'yyyyMMdd.hhmmss' to the final XPI file

# Suffix Timestamp to XPI Filename Walkthrough

The script to package and bundle the Firefox WebExtension has been updated.

## Changes Made

### 1. package.json script change
- Updated the `"package"` script in [package.json](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/package.json).
- Implemented an inline Node.js script that:
  - Formats a date timestamp using `getFullYear()`, `getMonth()`, `getDate()`, `getHours()`, `getMinutes()`, and `getSeconds()`.
  - Ensures correct padding for month, day, hours, minutes, and seconds.
  - Suffixes the formatted string to the `.xpi` file output name (e.g., `<name>_yyyyMMdd.hhmmss.xpi`).
