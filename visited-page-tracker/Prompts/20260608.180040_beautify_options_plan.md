# User Prompt
in the settings page, make it more presentable, with tabular columns for list of data, and border around different sections, etc...

# Beautify Options Page Plan

Improve the visual hierarchy and styling of the options page by adding column-based tables, status indicators, empty-state placeholders, and hover interactions to section cards.

## User Review Required

> [!NOTE]
> - The excluded sites list will be transformed into a dynamic table with column headers: "Website / Domain", "Status", and "Action".
> - Section cards will have interactive hover borders and subtle shadowing.

## Proposed Changes

### Options Page Layout & HTML

#### [MODIFY] [options.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/options/options.html)
- Replace the simple list element (`<ul id="excluded-sites-list">`) in the "Excluded Sites" section with a structured table layout (`.vpt-table-container` wrapper containing `<table>`, `<thead>`, and `<tbody id="excluded-sites-list">`).

### Options Page Styling

#### [MODIFY] [options.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/options/options.css)
- Style `.vpt-section` to have subtle shadow, transition, and a highlight border color on hover.
- Add stylesheet definitions for:
  - Table wrapper (`.vpt-table-container`), table cells (`.vpt-table-cell`), headers, and hover rows.
  - Custom status badge (`.vpt-badge--danger` for "Excluded").
  - Table buttons (`.vpt-btn-action`) with hover transitions.
  - Dashed-border empty-state wrapper (`.vpt-empty-state`).

### Options Controller Logic

#### [MODIFY] [options.ts](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/options/options.ts)
- Update `renderExcludedSites()` to dynamically build table rows (`<tr>` and `<td>` cells) instead of list items (`<li>`).
- Add display toggling for empty states vs tables.

## Verification Plan

### Manual Verification
1. Open Options page.
2. Verify that the sections have border hover states (changing borders slightly to accent blue on hover).
3. If no sites are excluded:
   - Verify that the empty state is displayed with a dashed border.
4. Add a site (e.g., `google.com`).
   - Verify that the table appears with columns: "Website / Domain", "Status" (displaying a red "Excluded" badge), and "Action" (displaying a "Remove" button).
5. Hover over a table row to verify alternating/hover background color.
6. Click "Remove" and verify the row disappears and the empty state is restored when the list becomes empty.
