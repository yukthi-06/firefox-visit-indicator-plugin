# User Prompt
on each exluded link in settings, provide an edit button, so that user can add or remove wildcards

# Walkthrough: Support Editing Excluded Sites/Patterns in Settings

We have implemented inline editing of exclusion patterns in the options page table.

## Changes Made

### 1. Options Page Logic
- Added `editingIndex` state variable in [options.ts](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/options/options.ts).
- Updated `renderExcludedSites()` to render an input field and "Save"/"Cancel" buttons for the row matching `editingIndex`.
- Added keyboard support to save changes on `Enter` and cancel on `Escape`.
- Implemented full input validation (empty state, RegExp check, duplicate checks against other entries) during save.

### 2. Styling
- Styled the inline inputs, success buttons, and layout alignments in [options.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/options/options.css).
