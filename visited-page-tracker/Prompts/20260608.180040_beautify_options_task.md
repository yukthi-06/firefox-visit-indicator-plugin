# User Prompt
in the settings page, make it more presentable, with tabular columns for list of data, and border around different sections, etc...

# Beautify Options Page Tasks

- [x] Update `options/options.html` to define the table structure and wrapper classes
- [x] Add CSS stylesheet classes for cards, tables, badges, empty states, and hover effects in `options/options.css`
- [x] Update options controller script `options/options.ts` to output `<tr>`/`<td>` and toggle empty state visibility
- [x] Verify manually that everything is styled correctly
