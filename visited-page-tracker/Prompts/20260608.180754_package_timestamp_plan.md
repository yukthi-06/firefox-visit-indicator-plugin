# User Prompt
suffix timestamp like 'yyyyMMdd.hhmmss' to the final XPI file

# Suffix Timestamp to XPI Filename Plan

This change adds a timestamp suffix in the format `yyyyMMdd.hhmmss` to the final `.xpi` file generated when packaging the extension.

## Proposed Changes

### Configuration / Scripts

#### [MODIFY] [package.json](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-visit-indicator-xpi_GITHUB/visited-page-tracker/package.json)
- Modify the `"package"` script to execute an inline Node.js snippet. This snippet generates the `yyyyMMdd.hhmmss` timestamp dynamically using local date/time and replaces the extension of the `.zip` file produced by `web-ext` with `_<timestamp>.xpi`.

## Verification Plan
- Verify package script formatting works under standard execution (e.g. valid Date properties concatenation).
