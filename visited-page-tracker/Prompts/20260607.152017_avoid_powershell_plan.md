# User Prompt
avoid powershell in npm commands

# Plan: Avoid PowerShell in NPM commands

We will replace the PowerShell renaming command in the `"package"` script with a platform-independent Node.js command.

## Proposed Changes

### visited-page-tracker

#### [MODIFY] [package.json](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-visit-indicator-plugin-xpi_GITHUB/visited-page-tracker/package.json)
- Modify `"package"` script: Replace powershell rename commands with node-based fs renaming code.

## Verification Plan
- Run `npm run package` and verify that the target zip file is built and renamed to `.xpi`.
