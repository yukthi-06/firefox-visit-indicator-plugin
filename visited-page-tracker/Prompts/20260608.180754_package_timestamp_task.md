# User Prompt
suffix timestamp like 'yyyyMMdd.hhmmss' to the final XPI file

# Suffix Timestamp to XPI Filename Tasks

- [x] Modify the `"package"` script inside `package.json` to generate the `yyyyMMdd.hhmmss` formatted timestamp and suffix it to the final `.xpi` file.
- [x] Verify code formatting for the Node expression is valid.
